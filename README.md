# ttmediabot-docker-helper
A lightweight helper script to run TTMediaBot in Docker. Automatically installs Docker, pulls prebuilt TTMediaBot images, and manages multiple bot instances with simple commands. No manual image building required.
